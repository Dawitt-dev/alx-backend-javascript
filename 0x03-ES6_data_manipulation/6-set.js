// Define the function setFromArray
function setFromArray(array) {
  // Create and return a new Set from the array
  return new Set(array);
}

// Export the function (if using ES6 modules)
export default setFromArray;
